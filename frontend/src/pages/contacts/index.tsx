import { NextPage } from "next";
import { Layout } from "~/components/layout/layout";

const ContactsPage: NextPage = () => {
  return <Layout title="Контакты">Contacts Page</Layout>;
};

export default ContactsPage;
