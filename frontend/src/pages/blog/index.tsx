import { NextPage } from "next";
import { Layout } from "~/components/layout/layout";

const AboutPage: NextPage = () => {
  return <Layout title="Блог">Blog Page</Layout>;
};

export default AboutPage;
